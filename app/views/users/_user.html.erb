<li>
  <%= gravatar_for user, :size => 30 %>
  <div>
  <%= link_to user.name, user %>
  <% if current_user.admin?  %>
   | <%= link_to "Delete", user, :method => :delete, :confirm => "Are you sure", :title => "Delete #{user.name}", :class => "delete"  %>
  <% end %><br/>
  <small><%= user.email %></small>
  </div>
</li>
